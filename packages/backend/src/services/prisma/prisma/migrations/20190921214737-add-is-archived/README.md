# Migration `20190921214737-add-is-archived`

This migration has been generated by Zane at 9/21/2019, 9:47:37 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "phoenix_db"."Chat" ADD COLUMN "isArchived" boolean NOT NULL DEFAULT false ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190919235658-i-dont-get-migrations..20190921214737-add-is-archived
--- datamodel.dml
+++ datamodel.dml
@@ -31,21 +31,22 @@
   id        String      @default(cuid()) @id
   chat      Chat
   content   String
   createdAt DateTime    @default(now())
-  sender    User
+  sender    User?
   type      MessageType
 }
 model Chat {
-  id        String        @default(cuid()) @id
-  createdAt DateTime      @default(now())
-  members   User[]        @relation(name: "ChatMembers")
-  messages  ChatMessage[]
-  name      String
-  owner     User          @relation(name: "ChatAdmin")
-  picture   String
-  updatedAt DateTime      @updatedAt
+  id         String        @default(cuid()) @id
+  createdAt  DateTime      @default(now())
+  members    User[]        @relation(name: "ChatMembers")
+  messages   ChatMessage[]
+  name       String
+  owner      User          @relation(name: "ChatAdmin")
+  picture    String
+  updatedAt  DateTime      @updatedAt
+  isArchived Boolean       @default(false)
 }
 model User {
   id         String   @default(cuid()) @id
```

## Photon Usage

You can use a specific Photon built for this migration (20190921214737-add-is-archived)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190921214737-add-is-archived'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
